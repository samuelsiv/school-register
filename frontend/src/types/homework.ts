export type Homework = {
    homeworkId: number,
    classId: number,
    subjectId: number,
    teacherId: number,
    title: string,
    description: string,
    createdAt: string,
    dueDate: string
}